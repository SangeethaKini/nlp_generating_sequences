# nlp_generating_sequences
 

# for data preprocessing and loading dataset inside the conda environment - Javeria Raja

conda install -c conda-forge spacy datasets -y
python -m spacy download en_core_web_sm

# end install commands data preprocessing - Javeria Raja